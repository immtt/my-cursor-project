---
description: 报错类问题用 Debug 排查，验证通过后再写入报错记录文档
alwaysApply: true
---

# 报错排查与文档记录

当用户反馈 **报错、异常、控制台错误、接口失败** 等需要定位根因的问题时：

1. **优先用 Debug 方式处理**：先复现或根据日志/Network 建立证据链，再改代码；避免未验证就宣称「已解决」。
2. **写入 `docs/报错记录与排错指南.md` 的时机**：仅在 **同一复现路径下已验证** 现象消失或变为明确的业务错误响应后，再新增或更新条目（含现象、场景、原因、修复手段、相关文件）。
3. **未验收前**：可在对话或提交信息里写「待用户复测」，**不要**在排错文档里写已结案式结论。

全文流程说明见 `docs/报错记录与排错指南.md` 中的 **「排查与入库流程」**。
