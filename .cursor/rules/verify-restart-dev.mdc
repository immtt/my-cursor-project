---
description: 修改需浏览器验收的代码后自动重启本机前后端，让用户可直接验证
alwaysApply: true
---

# 验收前重启开发环境

当本次改动涉及 `backend/`、`frontend/` 或 `scripts/start-dev.sh` / `restart-dev.sh`，且用户需要打开页面验收时：

1. **在项目根目录执行** `./scripts/restart-dev.sh`（会先结束占用默认端口的后端/前端进程，再拉起新进程）。使用终端工具时请求 **network** 与 **all** 权限，以便 `kill` 与监听端口。
2. 在后台运行即可；完成后明确告知用户：**前端** `http://127.0.0.1:${FRONTEND_PORT:-5173}`，**健康检查** `http://127.0.0.1:${BACKEND_PORT:-8080}/health`。
3. 若仅改文档、纯注释或与运行无关的文件，且用户未要求验收，可不必重启。

用户希望减少手动步骤；**不要**只改代码而不处理环境，除非用户明确说不用起服务。
